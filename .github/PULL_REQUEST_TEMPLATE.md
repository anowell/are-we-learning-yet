<!--
A one-line entry is a complete contribution. What you leave out just means the entry starts in
"New & experimental" while the automated signals catch up, which is where everything starts.

Curated data lives in `_data/crates.yaml` and `_data/apps.yaml`; everything measurable is scraped
on every build, so don't type numbers into either. The whole rubric: https://arewelearningyet.com/about/
-->

**What is it?** <!-- crate / app / learning resource / talk / correction -->

**Name and link:**

**Where it goes:** <!-- a section id from _data/topics.yaml or _data/apps.yaml, or just say where
you'd expect to find it -->

**Anything to point at?** <!-- optional: who else uses it, a post, a talk, a benchmark, a named
production user, or the issue behind a correction -->

**LLM-assisted?** <!-- optional: yes / no / partly. Disclosure, not detection — nothing is rejected
for the answer and no classifier runs here. -->

<!--
Two things the build rejects: `tier: archive` by hand for anything a signal already detects (a yank,
an archived or missing repo, a RustSec advisory, two silent years — a project that MOVED wants a
corrected `repository:` instead), and `tier: featured` without prose and a link somebody other than
the author wrote.
-->
